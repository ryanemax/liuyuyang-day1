import { PmjButtonDirective } from './pmj-button.directive';

describe('PmjButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PmjButtonDirective();
    expect(directive).toBeTruthy();
  });
});
