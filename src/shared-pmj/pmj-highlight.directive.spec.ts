import { PmjHighlightDirective } from './pmj-highlight.directive';

describe('PmjHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PmjHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
