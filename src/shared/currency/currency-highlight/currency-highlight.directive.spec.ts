import { CurrencyHighlightDirective } from './currency-Highlight.directive';

describe('CurrencyHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
