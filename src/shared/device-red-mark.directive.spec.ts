import { DeviceRedMarkDirective } from './device-red-mark.directive';

describe('DeviceRedMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceRedMarkDirective();
    expect(directive).toBeTruthy();
  });
});
