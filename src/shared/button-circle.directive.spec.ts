import { ButtonCircleDirective } from './button-circle.directive';

describe('ButtonCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonCircleDirective();
    expect(directive).toBeTruthy();
  });
});
