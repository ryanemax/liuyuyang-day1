import { AccoundCardDirective } from './accound-card.directive';

describe('AccoundCardDirective', () => {
  it('should create an instance', () => {
    const directive = new AccoundCardDirective();
    expect(directive).toBeTruthy();
  });
});
