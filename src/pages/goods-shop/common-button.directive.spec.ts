import { CommonButtonDirective } from './common-button.directive';

describe('CommonButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonButtonDirective();
    expect(directive).toBeTruthy();
  });
});
