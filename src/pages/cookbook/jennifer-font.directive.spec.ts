import { JenniferFontDirective } from './jennifer-font.directive';

describe('JenniferFontDirective', () => {
  it('should create an instance', () => {
    const directive = new JenniferFontDirective();
    expect(directive).toBeTruthy();
  });
});
