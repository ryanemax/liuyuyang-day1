import { BtnFastMailDirective } from './btn-fast-mail.directive';

describe('BtnFastMailDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnFastMailDirective();
    expect(directive).toBeTruthy();
  });
});
