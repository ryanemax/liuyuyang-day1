import { CircleButtonDirective } from './circle-button.directive';

describe('CircleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
