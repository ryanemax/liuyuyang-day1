import { StatusFinishPipe } from './status-finish.pipe';

describe('StatusFinishPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusFinishPipe();
    expect(pipe).toBeTruthy();
  });
});
