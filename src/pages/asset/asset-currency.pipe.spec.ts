import { AssetCurrencyPipe } from './asset-currency.pipe';

describe('AssetCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
