import { AssetButtonDirective } from './asset-button.directive';

describe('AssetButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AssetButtonDirective();
    expect(directive).toBeTruthy();
  });
});
